import { StyleSheet } from 'react-native';
import colors from '../../theme/colors';

export default StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 10,
  },
  indicator: {
    flex: 1,
    alignSelf: 'center',
  }
});
